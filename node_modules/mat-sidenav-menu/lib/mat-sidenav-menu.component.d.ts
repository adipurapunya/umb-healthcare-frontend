import { QueryList } from '@angular/core';
import { Router, ActivatedRoute } from '@angular/router';
import { MatSidenav } from '@angular/material';
import { SideNavItemComponent } from './item';
export declare class MatSidenavMenuComponent {
    private router;
    private aroute;
    sidenav: MatSidenav;
    title: '';
    items: QueryList<SideNavItemComponent>;
    active: SideNavItemComponent;
    constructor(router: Router, aroute: ActivatedRoute);
    private getPath();
}
